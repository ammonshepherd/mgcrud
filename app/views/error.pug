extends layout

block content
  h1= message
  #error-action
    p You can't delete this item because another item relies on it.
    h2 Start over: 
      a(href='/') Home
  #error-details.card-panel.red.lighten-3
    h3 Error Details
    p= error.detail
    pre #{error.stack}
